# --------------------------------------------------------------------
# Task queue & Redis broker
# --------------------------------------------------------------------
taskiq==0.11.17
taskiq-redis==1.0.9
redis==5.3.0
asyncpg==0.30.0
h2==4.2.0
httpx==0.28.1
# --------------------------------------------------------------------
# Google Cloud clients used in tasks.py
# --------------------------------------------------------------------
google-cloud-firestore==2.21.0
google-cloud-pubsub==2.30.0          # pulls google-cloud-core==2.4.3
google-cloud-storage==2.19.0         # GCS attachments in tasks
SQLAlchemy==2.0.41                   # DB engine via common.db_conn

# --------------------------------------------------------------------
# ADK client helpers
# --------------------------------------------------------------------
google-adk==1.5.0                    # ensure_session, run_agent_stream, create_session

# --------------------------------------------------------------------
# Minimal FastAPI shim for Cloud Run /healthz
# --------------------------------------------------------------------
fastapi==0.115.14
uvicorn==0.35.0

# --------------------------------------------------------------------
# Local‑dev convenience (env‑file support)
# --------------------------------------------------------------------
python-dotenv==1.1.1

google-cloud-speech==2.33.0
