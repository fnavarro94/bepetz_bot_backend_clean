# ── Web framework & SSE support ───────────────────────────────
fastapi==0.115.14
uvicorn==0.35.0
sse-starlette==2.3.6          # EventSourceResponse

# ── Async Redis client & fan‑out queue ────────────────────────
redis==5.3.0                  # provides redis.asyncio
janus==2.0.0              # sync/async queue bridge

# ── CORS middleware (pulled in by FastAPI, but pinned explicitly)
starlette==0.46.2             # matches fastapi version
python-dotenv